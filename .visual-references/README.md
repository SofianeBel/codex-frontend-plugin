# Visual References

Reference mockups live here so the frontend app cannot serve them as normal assets.

Scripts can read this folder for visual comparison, but generated UI code must not import, copy, or render these files.
